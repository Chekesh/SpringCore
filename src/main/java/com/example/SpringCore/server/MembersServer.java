package com.example.SpringCore.server;


import org.springframework.stereotype.Service;

@Service
public class MembersServer {
/*
    @Autowired
    private MembersRepository membersRepository;

    public void MembersServer(MembersRepository membersRepository) {
        this.membersRepository = membersRepository;
    }

    public List<Members> getAllMembers(){
        List<Members> members = new ArrayList<>();
        membersRepository.findAll().forEach(members::add);
        return members;
    }

    public ResponseEntity<List<Members>> ReturnData(@RequestBody MembersRequest student){
        List<Members> all = membersRepository.findAll();
        return ResponseEntity.ok(all);
    }*/



}
